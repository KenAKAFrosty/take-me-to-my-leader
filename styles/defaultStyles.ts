import { CSSProperties } from "react"

export const defaultPageContainer: CSSProperties = {
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-between",
    minHeight: "100vh"
}

export const defaultMainContainer: CSSProperties = {
    flexGrow: 1,
    display:"flex",
    alignItems: "center"
} 